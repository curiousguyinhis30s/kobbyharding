# Application Configuration
VITE_APP_TITLE=K Harding Classics
VITE_ENV=development

# API (when backend is ready)
VITE_API_URL=http://localhost:3000/api
VITE_PUBLIC_URL=http://localhost:9000

# Stripe Configuration (replace with real keys for production)
# NEVER commit real secret keys to version control
VITE_STRIPE_PUBLIC_KEY=pk_test_xxx

# Analytics (optional)
VITE_GA_TRACKING_ID=UA-XXXXXXXXX-X
VITE_GTM_ID=GTM-XXXXXXX

# Feature Flags
VITE_ENABLE_AI_CHAT=true
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_FESTIVAL_PICKUP=true

# ============================================
# SECURITY CONFIGURATION
# ============================================

# DEMO CREDENTIALS (Development/Testing Only)
# These are secure hashed passwords using SHA-256
# In production, implement server-side authentication with bcrypt/argon2

# Admin Login:
#   Email: admin@kobysthreads.com
#   Password: SecureAdmin2025!

# User Login:
#   Email: john@example.com or sarah@example.com
#   Password: SecureUser2025!

# IMPORTANT SECURITY NOTES:
# 1. Never store passwords in environment variables
# 2. Never commit .env files with real credentials to Git
# 3. Use .gitignore to exclude .env files
# 4. In production, use server-side authentication
# 5. Passwords are hashed using SHA-256 (client-side demo only)
# 6. Sessions expire on browser close (stored in sessionStorage)
# 7. Only minimal user data stored in localStorage
